# 立体防御

在 B 站做**范围拉黑**的用户脚本。用一句话描述你不想再看到的人，它会把这句口语编译成可执行的判定规则，跨多个数据源采集证据、逐条打分，最后由你复核后批量拉黑。

不是关键词黑名单，也不是「按 TAG 屏蔽」。判定的依据是**立场与话术**，不是话题——做国际时事的 UP 会大量出现相关词汇，但语境是揭露与批判，不应该被误伤。

## 为什么是用户脚本

`api.bilibili.com` 的响应不带 `Access-Control-Allow-Origin`，任何部署在自己域名下的静态页面都无法直连它，也拿不到同站的 `SESSDATA` / `bili_jct` Cookie。用户脚本运行在 `bilibili.com` 页面内，通过 `GM_xmlhttpRequest` 发起请求，Cookie、Referer、风控指纹都是天然的，是**唯一无需后端的可行形态**。

同一份代码也能构建成独立静态页面（`dist-web/`），但它需要你在设置里填一个转发地址才能连通接口，否则只作为界面预览。

## 安装

1. 安装 [Tampermonkey](https://www.tampermonkey.net/) 或 Violentmonkey。
2. 从 [Releases](../../releases) 下载 `stereo-defense.user.js`，或在浏览器里直接打开该文件。
3. 打开任意 `bilibili.com` 页面，右下角出现「防御」入口；首次使用会自动展开面板。

登录 B 站后无需任何配置即可读取账号；`CSRF` 会从 `bili_jct` Cookie 自动取得。

## 使用

| 步骤 | 说明 |
| --- | --- |
| 账号 | 自动检测登录状态，读取昵称与 UID |
| 画像 | 用口语写下你的偏好，点「生成画像」，得到可编辑的判定规则 |
| 扫描 | 设定范围后开始采集，过程中可随时停止 |
| 复核 | 逐条查看得分与命中依据，调整阈值，确认后批量拉黑 |
| 黑名单 | 同步线上黑名单，可逐条解除 |

拉黑默认**只在你确认后执行**。扫描结果本身不会修改任何账号状态。

## 判定原理

### 双引擎提炼画像

- **配置了 API**：调用任意 OpenAI 兼容接口，把口语编译成结构化规则。界面实时显示 token 用量与按单价估算的费用；接口返回的 `usage` 优先于本地估算。
- **未配置 API**：使用内置的小模型 `bge-small-zh-v1.5`（ONNX 量化，运行时从 CDN 按需加载）做语义比对，配合内置中文立场词库。模型不可用时自动降级为纯词表引擎，功能不中断。

两者产出同一份结构：

| 字段 | 作用 |
| --- | --- |
| `target` | 目标人群的客观描述，作为语义锚点 |
| `topics` | 检索用的领域词，**只决定去哪里找，不决定拉黑谁** |
| `markers` | 目标人群自己会说的话术 |
| `exemplars` | 典型发言，用于捕捉「意思相同但用词不同」的变体 |
| `guards` | 反驳、批判、科普语境词 |
| `allow` | 应当放行的账号特征 |

### 防误伤是设计的核心

1. **标记词只收「目标自己会说的」**。批评者用来骂他们的话（例如目标是崇洋媚外者时的「殖人」「慕洋犬」「公知」）一律进 `guards`。收错方向会把批评者一起拉黑。
2. **反驳语境降权**。每个标记词命中时，检查前后 16 字的窗口；出现 `guards` 词则该次命中的权重降到 12%。这直接处理了「批判台独的视频里满是『台独』二字」的情况。
3. **话题与立场分离**。只有话题重合、没有立场信号的内容，得分被硬性压到 0.22 以下。
4. **词表命中但语义不符**时再打 45% 折扣，应对引用、转述、反讽。
5. **多源印证**。单一来源即使高分也只能进复核区；需要 ≥2 条独立证据或 ≥2 类来源才能进入建议拉黑。
6. **弱来源封顶**。他人评价 ≤0.70、关注列表 ≤0.55、昵称 ≤0.45，保证它们只能做佐证，无法单独定案。
7. **机构与官方认证账号**自动降级并标记，需人工确认。
8. **归一化匹配**。去掉空白与符号后再比对，「台 独」「台\*独」这类规避写法同样命中。
9. **可选 AI 复核**。配置 API 后，可让模型对边界候选逐条裁决。

### 数据来源

| 来源 | 接口 | 说明 |
| --- | --- | --- |
| 视频 | `/x/space/wbi/arc/search` | 标题、简介、分区 |
| 动态 | `/x/polymer/web-dynamic/desktop/v1/feed/space` | 正文与图文 |
| 专栏 | `/x/space/article` | 标题与摘要 |
| 评论 | `/x/v2/reply/wbi/main` | 视频评论区，含楼中楼 |
| 他人评价 | 同上 | 高赞评论单独作为佐证 |
| 关注列表 | `/x/relation/followings` | 判断「成分」的交叉信号 |
| 账号状态 | `/x/web-interface/card` | 封禁状态、认证、粉丝数、签名 |
| 发现 | `/x/web-interface/search/type` | 按领域词检索视频与用户 |

**评论区是覆盖「不发视频但评论一股味」这类账号的关键**：扫描会读取目标视频的评论区，对每条评论的作者单独打分。这类账号没有投稿、没有动态，只有在评论区才能被看到。

## 已知边界

- **B 站没有「按 mid 查询该用户全站评论」的公开接口**。已实测穷举 `/x/v2/reply/user`、`/me`、`/space/comment`、`search_type=comment` 等路径，全部 404 或降级。因此历史评论只能通过两条途径覆盖：扫描已知视频的评论区（官方、可靠），或开启「历史评论补全」走第三方索引（非官方、覆盖不全、可用性不稳定，默认关闭）。
- **风控**。实测同一出口 IP 连续约 25 次快速请求即触发 `-412`，需冷却约 60 秒。默认串行、间隔 1.2 秒，遇到 `-352` / `-412` / `-799` 自动退避。扫描规模越大越容易触发，建议分批进行。
- **`/x/space/wbi/acc/info` 需要登录态**，即使签名正确也可能返回 `-352`，因此用户资料一律走免签的 `/x/web-interface/card`。
- **静态页面版无法直连接口**，这是 CORS 的硬限制，不是缺陷。

## 开发

```bash
pnpm install
pnpm dev            # 开发服务器（静态页面形态）
pnpm typecheck      # 类型检查
pnpm build          # 构建用户脚本 -> dist/stereo-defense.user.js
pnpm build:web      # 构建静态页面 -> dist-web/
pnpm build:all      # 两者都构建
pnpm preview        # 预览静态页面
pnpm smoke          # 用本机 Chrome 做渲染冒烟测试（需先 pnpm preview）
```

### 结构

```
src/
  main.tsx                用户脚本入口：Shadow DOM 挂载、悬浮入口
  main.web.tsx            静态页面入口
  app/                    界面
    ui.tsx                基础组件
    views/                账号 / 画像 / 扫描 / 复核 / 黑名单 / 设置
  lib/
    bili/                 接口层
      wbi.ts              WBI 签名
      md5.ts              签名用的 MD5
      transport.ts        通道：GM_xmlhttpRequest / fetch / 转发
      queue.ts            并发与限速
      api.ts              端点封装
    analyze/              判定层
      lexicon.ts          中文立场词库与反驳语境匹配
      segment.ts          Intl.Segmenter 切词
      embed.ts            内置小模型
      local.ts            无 API 时的画像构建
      score.ts            打分与汇总
    llm/                  大模型层：提示词、计费、调用
    scan.ts               采集编排
    store.ts              状态与持久化
```

界面使用 React 19 + Tailwind CSS 4，构建使用 Vite 8 + vite-plugin-monkey，状态使用 Zustand。用户脚本内联 SystemJS，不依赖任何外部 CDN 才能启动。

静态页面可发布到 GitHub Pages：在仓库 Settings → Pages 中选择 **GitHub Actions** 作为来源，推送到 `main` 即会自动构建部署。

## 许可

MIT
